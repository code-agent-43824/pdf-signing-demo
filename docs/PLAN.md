# План

Этап: сопровождение — Рутокен в Firefox, решение по шрифтам, документация на
русском.

- [~] Рутокен в Firefox: разрешить в CSP по SHA-256 два inline-скрипта
  Firefox-расширения «Адаптер Рутокен Плагин», закрепить тестом и процедурой
  обновления, задеплоить и проверить production.
- [ ] Владелец проверяет подпись реальным Рутокеном в Firefox.
- [x] Записать решение владельца о шрифтах штампа в `CLAUDE.md`.
- [ ] Перевести документацию проекта на русский, убрать расхождения с кодом,
  утверждения о состоянии из медленных документов и ошибки.

## Замечено попутно

- [ ] Тест `isolated worker is asynchronous and timeout kills its process group`
  (`test/runtime-controls.test.js`) нестабилен в облачной песочнице Claude Code:
  проверка завершения процесса должна считать зомби завершённым.
- [ ] Если `initialize()` расширения Рутокена не завершается, инициализация
  навсегда остаётся в состоянии «Проверка…»: `await root.rutoken.ready` в
  `public/modules/rutoken-adapter.js` ждёт без таймаута.
