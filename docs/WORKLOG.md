# Журнал работ

Что собирались сделать, что сделано и что дальше. Новые записи сверху.

## 2026-09-24 — ожидание CryptoPro, таймаут Рутокена, гейты CI

**План.** Владелец принял три пункта «Замечено попутно» из `docs/PLAN.md`.
Кроме того, он попросил прекращать ожидание CryptoPro, когда пользователь
переключается на Рутокен. По умолчанию выбран CryptoPro; если его расширения
нет, через некоторое время появляется затемнённый попап «плагин не
установлен» — даже когда пользователь сразу переключился на Рутокен.

Разбор (подробно — в `docs/JOURNAL.md`):

- Попап рисует вендорный `cadesplugin_api.js`. При загрузке он ставит таймер
  (`set_load_timeout`), и если к его срабатыванию плагин не ответил, в
  Firefox показывает оверлей `#cadesplugin_ovr`
  (`show_firefox_missing_extension_dialog`). Отменить таймер снаружи нельзя,
  но оверлей не рисуется, если в момент срабатывания установлен
  `window.cadesplugin_skip_extension_install`.
- Сохранённый выбор плагина не работает с первого коммита.
  `getSavedCryptoStack()` в `public/app.js` сначала читает отмеченную
  радиокнопку, а в `public/index.html` у CryptoPro стоит `checked`, так что до
  `localStorage` очередь не доходит. Каждый новый заход начинается с CryptoPro
  и запускает его таймер.
- Оба эффекта воспроизведены в Firefox 156 без расширения CryptoPro.

1. Тест `isolated worker…`: проверка «процесс завершён» считает зомби
   (состояние `Z` в `/proc/<pid>/stat`) завершённым процессом.
2. CryptoPro. Сохранённый выбор читается раньше значения по умолчанию из
   разметки. `initialize()` адаптера CryptoPro принимает `AbortSignal`:
   переключение на Рутокен прерывает ожидание и ставит
   `cadesplugin_skip_extension_install`. Пока CryptoPro активен, вендорный
   загрузчик ведёт себя как раньше. Тесты адаптера и вендорного загрузчика —
   в `node:vm`, проверка — в Firefox.
3. Рутокен. Ответы расширения и плагина при инициализации (`rutoken.ready`,
   `isExtensionInstalled`, `isPluginInstalled`, `loadPlugin`) ограничены тем же
   сроком, что таймер загрузчика CryptoPro. По истечении срока на плашке
   появляется ошибка, а не вечное «Проверка…».
4. Поиск секретов: gitleaks 8.30.1 по всей истории в CI. Релизный архив
   скачивает скрипт, SHA-256 закреплён в нём же. Локальный прогон по 138
   коммитам находок не дал.
5. Lint и форматирование: Biome 2.5.14 (devDependency) для JS, Ruff 0.16.8
   (отдельный lock с хешами) для Python; `npm run lint` входит в
   `npm run verify`. Код форматируется один раз отдельным коммитом. На сервер
   инструменты не попадают: деплой ставит `npm ci --omit=dev`, а
   `scripts/verify-release.sh` lint не запускает.
6. Каждый шаг, меняющий рантайм, проходит CI и деплой; после деплоя — лог
   job, `health/ready` и соседний сайт.

**Сделано.**

- Шаг 1. `processIsRunning()` в `test/runtime-controls.test.js` считает
  процесс в состоянии `Z` или `X` остановленным; без procfs остаётся проверка
  сигналом 0. В песочнице внук после убийства группы ещё 1,7–2,0 с числился
  живым по сигналу 0, а новая проверка видела его остановленным через 0–1 мс.
  Тест прошёл 15 раз подряд. Попутно выяснилось, что тест не ловил поломку,
  которую называет: если убивать только прямого потомка, он всё равно
  проходил, только через 60 с. Добавлена проверка, что операция завершилась
  быстрее 10 с (подробно — в `docs/JOURNAL.md`).

## 2026-09-24 — Рутокен в Firefox, шрифты, документация

**План.** Владелец сообщил, что приложение перестало нормально работать с
Рутокен Плагином, и подозревал изоляцию плагинов (PR-10j, `3011186` и
`159f94d`). По его ответам: приложение не видит расширение и плагин, браузер —
Firefox, когда Рутокен работал в последний раз, неизвестно. Кроме того, шрифты
штампа остаются в репозитории (решение владельца), ветку
`claude/vibrant-curie-u8l0q9` владелец удалил, а документацию нужно перевести
на русский и убрать из неё расхождения и ошибки.

Разбор (подробно — в `docs/JOURNAL.md`):

- PR-10j поведения не менял. Код сравнён с `7e80d53`, в Chromium с фейковым
  расширением прогнано 9 сценариев — результаты одинаковые. Production отдаёт
  ровно файлы `main`, серверная обработка CMS после 31.07 не менялась.
- Причина — CSP из PR-8 (`efd707a`, 30.07). Firefox-сборка расширения (MV2,
  1.0.5.0) внедряет свой API двумя inline-скриптами, а CSP страницы без
  `'unsafe-inline'` их блокирует. В Firefox 156 с опубликованным XPI это
  воспроизведено; с SHA-256 этих двух скриптов в `script-src` расширение
  работает. Chrome-сборка (MV3, 1.1.0.0) внедряет API вне CSP страницы и в
  Chromium работает и с текущим CSP, и с исправленным.

1. Добавить оба хеша в `script-src`, закрепить тестом, добавить инструмент
   пересчёта хешей и процедуру обновления в `docs/VENDOR_ASSETS.md`;
   задеплоить и проверить production в Firefox с расширением.
2. Владелец проверяет подпись реальным токеном в Firefox.
3. Записать решение о шрифтах в `CLAUDE.md`.
4. Перевести документацию и убрать расхождения.

**Сделано.**

- Шаг 1 (код): в `script-src` добавлены два хеша
  (`RUTOKEN_FIREFOX_EXTENSION_SCRIPT_HASHES` в `src/application.js`), проверка
  CSP в `test/server-surface.test.js` сравнивает точное значение. Новый
  `scripts/rutoken-firefox-csp-hashes.js` выполняет `content.js` расширения на
  заглушках DOM. На опубликованном XPI 1.0.5.0 он выдал ровно проверенные в
  Firefox хеши; его логику покрывает `test/rutoken-firefox-csp.test.js`.
  Происхождение и процедура обновления — в `docs/VENDOR_ASSETS.md`,
  исключение из запрета inline-скриптов отражено в `README.md` и `CLAUDE.md`.
- Шаг 1 (раскатка): CI для `83fb7e6` прошёл целиком и задеплоил релиз. По
  логу деплоя: на сервере Python 3.14, 75/75 тестов, `npm audit` и `pip check`
  чистые; canary вернул `valid/not_checked/not_checked` и pyHanko
  `intact/valid/trusted/ENTIRE_FILE`; `deployed 83fb7e6…`, backup
  `20260924T164902Z-cicd-83fb7e69af23`. Снаружи CSP содержит оба хеша,
  `health/ready` — 200, `health/metrics` — 404, UI и соседний сайт — 200.
  Firefox 156 с опубликованным XPI на production: объект расширения есть,
  нарушений CSP нет, «Расширение Рутокен — доступно». Чтобы Firefox в
  песочнице доверял прокси, CA прокси подключены через
  `distribution/policies.json`; проверка TLS не отключалась.
- Шаг 3: в разделе «Departures» в `CLAUDE.md` шрифты штампа записаны как
  решение владельца.
- Шаг 4, перевод: на русский переведены `docs/SUPPLY_CHAIN.md`,
  `docs/VENDOR_ASSETS.md`, `test/fixtures/README.md`, README spike и
  `public/assets/fonts/stamp/LICENSES.md`. Текст лицензии
  `public/vendor/LICENSE.rutoken-plugin.txt` оставлен в оригинале, `AGENTS.md`
  и `CLAUDE.md` по своду остаются на английском. Попутно исправлены:
  - порядок гейтов CI в `docs/SUPPLY_CHAIN.md`;
  - утверждение spike, будто `addEssCert` не описан в публичном API Рутокена
    (он описан, по умолчанию `false`).

  Копии значений из кода (SHA-256 и SRI vendor-файлов, версии Node и npm)
  заменены ссылками на `SHA256SUMS`, `CRYPTO_SCRIPTS`, `.node-version` и
  `packageManager`.

- Шаг 4, `README.md`: утверждения о состоянии («реализация ещё не начата»,
  «Что уже есть», «больше не используют») заменены ссылками на
  `docs/STATUS.md`, английские куски переведены. Исправлены расхождения с
  кодом:
  - в списках не хватало модулей фронтенда и `src/observability/`;
  - не были описаны `HTTP_HEADERS_TIMEOUT_MS`, `HTTP_REQUEST_TIMEOUT_MS` и
    `NODE_ENV`;
  - readiness проверяет ещё `prlimit` и очередь;
  - preview получает собственную CSP целиком, а не только правку
    anti-framing;
  - JSON обязателен только для POST;
  - unit сервиса деплой ставит сам, а не хранит как эталон;
  - команды golden-проверки не учитывали `.venv`.

  Значения по умолчанию и лимиты заменены ссылками на константы и файлы.
  В `CLAUDE.md` исправлена ошибка: окружение читают три места, а не одно.

- Шаг 4, планы. `docs/CADES_BES_PLAN.md`: статус этапа 0 и «этапы 1–5 не
  начаты» заменены ссылками на `docs/STATUS.md` и README spike; записано, что
  `addUserCertificate` и `addEssCert` описаны в публичном API Рутокена.
  `docs/REMEDIATION_PLAN.md`:
  - «Статус документа: реализация начата» заменён ссылкой на `docs/STATUS.md`;
  - в §10 снято ошибочное «реализовано»: в `ci.yml` нет гейтов
    lint/форматирования и поиска секретов. Пробел записан в `docs/STATUS.md`,
    решение — в `docs/PLAN.md`;
  - к записи PR-8 добавлена датированная поправка о Рутокене в Firefox.

  Датированные статусы в подразделах «Ход выполнения» оставлены: это история
  раскатки, а не текущее состояние.

**Дальше.** Шаг 2 — проверка владельцем подписи реальным Рутокеном в Firefox.
Пункты «Замечено попутно» в `docs/PLAN.md` — по решению владельца.

## 2026-09-24 — починка `npm audit` и переход на свод правил

**План.** Полный гейт `npm run verify` на `main` (`2b36d06`) падает на
`npm audit --omit=dev --audit-level=high`. После последнего зелёного CI (28.08)
опубликованы advisories для `fast-uri` 3.1.5 (high, приходит через `ajv`) и `qs`
6.15.3 (moderate, приходит через `express`). Любой новый CI-прогон `main` будет
красным, деплой заблокирован. Владелец решил сначала починить ствол, затем
подключить свод правил из `code-agent-43824/coding-rules`: работа только в `main`,
без веток. Ветку `claude/vibrant-curie-u8l0q9` с первой версией `CLAUDE.md`
владелец решил оставить.

1. Обновить в `package-lock.json` только `fast-uri` и `qs`, пересоздать SBOM
   закреплённым npm, прогнать `npm run verify`.
2. Запушить в `main`, проверить CI, деплой и production.
3. Положить `AGENTS.md` как есть, импорт в `CLAUDE.md` и карту кода на английском.

**Сделано.**

- Шаг 1: `npm update fast-uri qs --package-lock-only` под npm 10.9.8 поднял
  `fast-uri` до 3.1.8 и `qs` до 6.16.0 в пределах диапазонов `ajv` и `express`;
  новых пакетов нет. SBOM пересоздан, `npm run verify` зелёный: 73/73 тестов,
  `npm audit` — 0 уязвимостей.
- Попутно: CI-шаг `pip-audit` на том же `main` тоже красный — у `pypdf` 6.15.0
  три advisories (исправлены в 6.16.0 и 6.16.1). `pypdf` используют только
  `test/fixtures/generate.py` и `test/validate_corpus.py`, рантайм его не
  импортирует. Обновление — отдельным шагом по `docs/SUPPLY_CHAIN.md`.
- `pypdf`: пин в `requirements.in` поднят до 6.16.2 (последний патч ветки 6.16,
  закрывает все три advisories), `requirements.txt` пересоздан pip-tools на
  Python 3.12; в lock-файле изменилась только запись `pypdf`. Фикстуры
  воспроизводятся побайтно, `npm run verify` зелёный (73/73), `pip-audit` и
  `pip check` чистые. Прогон на production Python 3.14 выполнит
  `scripts/verify-release.sh` при деплое до переключения релиза.

- Шаг 2: CI-прогон `d3d0bc3` прошёл все гейты `npm run verify` и упал только
  на `pip-audit`, как и ожидалось. Прогон 57 (`4b279d2`) зелёный целиком и
  задеплоил релиз. По полному логу деплоя: на сервере Python 3.14 (колёса
  `cp314`), 73/73 тестов, `npm audit` и `pip check` чистые; canary вернул
  `integrity valid`, `trust`/`qualified` `not_checked` и pyHanko
  `intact/valid/trusted/ENTIRE_FILE`; скрипт напечатал `deployed 4b279d2…`,
  backup `20260924T134549Z-cicd-4b279d277791`. Снаружи: `health/ready` — 200,
  все проверки `true`; `health/metrics` — 404; UI и `https://mescheryakov.pro/`
  — 200. Прогон 58 (`b9c0a48`, правила) зелёный, деплой пропущен.
- Шаг 3: `install.sh` из `coding-rules` положил `AGENTS.md`, sha256 совпадает
  с каноническим. `CLAUDE.md` — заголовок, импорт `@AGENTS.md` и карта кода на
  английском: команды, устройство, ловушки, «Settled decisions» и «Departures»
  со ссылками на источники причин, версии, деплой, проверки владельца. Правила
  из `AGENTS.md` в нём не пересказываются, состояние вынесено в
  `docs/STATUS.md`. Причина хранить в git шрифты штампа нигде не записана; в
  «Departures» это отмечено как вопрос к владельцу.

**Дальше.** Пункты «Замечено попутно» в `docs/PLAN.md` — по решению владельца.
