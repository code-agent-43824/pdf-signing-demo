# Состояние

Проверено 2026-09-24.

- **Production.** `https://mescheryakov.pro/pdf-signing/` работает на релизе
  `2ec2843`. Публичный `health/ready` — 200, все проверки `true`; внешний
  `health/metrics` — 404; UI и соседний сайт `https://mescheryakov.pro/` — 200.
- **Рутокен в Firefox.** На production с опубликованным расширением «Адаптер
  Рутокен Плагин» 1.0.5.0 объект расширения создаётся, нарушений CSP нет,
  плашка «Расширение Рутокен — доступно». Подпись реальным токеном в Firefox
  ещё не проверена: для этого нужен компьютер владельца с установленным
  Рутокен Плагином.
- **Выбор плагина.** Сохраняется между заходами. После переключения на
  Рутокен оверлей CryptoPro «нужно расширение» не появляется: проверено на
  production в Firefox 156 без расширения CryptoPro. С установленным CryptoPro
  переключение не проверялось — нужен компьютер владельца.
- **Инициализация Рутокена.** Если расширение или плагин не отвечает,
  через `RESPONSE_TIMEOUT_MS` (`public/modules/rutoken-adapter.js`) вместо
  вечного «Проверка…» появляется ошибка. Проверено в Chromium со стендовым
  расширением; на реальном зависании не воспроизводилось.
- **Ствол `main`.** Зелёный. В CI работают все гейты из §10
  `docs/REMEDIATION_PLAN.md`: поиск секретов по всей истории, lint и
  форматирование JS и Python, тесты, `npm audit`, `pip-audit`, SBOM и
  фикстуры (`.github/workflows/ci.yml`, `docs/SUPPLY_CHAIN.md`).
- **Известные дефекты.** Нет.
- **CAdES-BES (`docs/CADES_BES_PLAN.md`).** Этап 0 не закрыт: вердикт spike —
  `PARTIAL`, прогона с реальными провайдерами на компьютере владельца в
  репозитории нет.
- **Где остановилась работа.** Всё, что можно сделать без компьютера
  владельца, сделано. Ждут проверки владельцем подпись реальным Рутокеном в
  Firefox и переключение плагинов при установленном CryptoPro
  (`docs/PLAN.md`).
