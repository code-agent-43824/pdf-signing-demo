# Known-good transitive closure from the production Python 3.14 environment.
# Keep this file exact so compiling on Python 3.12 and 3.14 resolves the same
# package versions; requirements.txt additionally pins artifact hashes.
certifi==2026.4.22
cffi==2.0.0
charset-normalizer==3.4.7
cryptography==50.0.0
idna==3.15
lxml==6.1.0
oscrypto==1.3.0
pycparser==3.0
pyhanko-certvalidator==0.30.2
PyYAML==6.0.3
requests==2.34.2
tzlocal==5.3.1
uritools==6.1.0
urllib3==2.7.0
